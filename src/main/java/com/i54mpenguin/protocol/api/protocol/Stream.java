package com.i54mpenguin.protocol.api.protocol;

/**
 * A stream means a data flow direction in the proxy model.<br>
 * UPSTREAM = Player to Proxy<br>
 * DOWNSTREAM = Server to Proxy
 */
public enum Stream {

    DOWNSTREAM, UPSTREAM

}
